"use strict";
/*Crie um array com 5 números fornecidos pelo usuário e mostre o array completo.*/
let numeros;
numeros = [1, 2, 3, 4, 5];
for (let i = 0; i < numeros.length; i++) {
    console.log(numeros[i]);
}
