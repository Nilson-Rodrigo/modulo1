/*Crie uma classe ContaBancaria com os atributos titular e saldo, e métodos depositar(valor: number) e sacar(valor: number).*/

class ContaBancaria {
  titular: string;
  saldo: number;
  constructor(titular:string, saldo:number){
    this.titular = titular;
    this.saldo = saldo;
  }
  public depositar(){
  }
  public sacar(){
  }
}