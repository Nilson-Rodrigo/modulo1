"use strict";
//2. Peça ao usuário um número inteiro positivo. Mostre todos os números pares de 0 até esse número.
let numero1;
let contar = 0;
numero1 = Number(prompt('digite um numero positivo'));
while (numero1 >= contar) {
    alert(contar);
    contar = contar + 2;
}
